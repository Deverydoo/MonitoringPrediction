# NordIQ Performance Enhancement Requirements
# Install these for 2-3× faster dashboard performance
#
# Installation:
#   pip install -r requirements_performance.txt
#
# Or individually:
#   pip install polars

# Polars - Fast DataFrame library (5-10× faster than pandas)
# Used in: heatmap.py, historical.py
# Impact: 50-100% faster data operations
polars>=0.19.0

# Note: Plotly WebGL (Scattergl) is already included in plotly
# No additional dependency needed for WebGL rendering
