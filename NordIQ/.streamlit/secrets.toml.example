# Streamlit Secrets Configuration
# Copy this file to secrets.toml and fill in your values
#
# IMPORTANT: secrets.toml should be added to .gitignore (DO NOT commit to git)
#
# For production deployment:
#   1. Copy this file: cp secrets.toml.example secrets.toml
#   2. Generate API key: openssl rand -hex 32
#   3. Set the api_key below to the generated key
#   4. Ensure the SAME key is set in daemon's TFT_API_KEY environment variable

[daemon]
# API key for authenticating with TFT Inference Daemon
# This MUST match the TFT_API_KEY environment variable on the daemon server
api_key = "your-secret-key-here"

# Daemon URL (optional - overrides default)
url = "http://localhost:8000"

# Example for remote deployment:
# [daemon]
# api_key = "abc123def456..."
# url = "https://monitoring.company.com:8000"
